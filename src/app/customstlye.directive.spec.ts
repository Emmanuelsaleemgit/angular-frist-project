import { CustomstlyeDirective } from './customstlye.directive';

describe('CustomstlyeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomstlyeDirective();
    expect(directive).toBeTruthy();
  });
});
